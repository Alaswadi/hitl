version: '3.8'

services:
  app:
    build: .
    container_name: mosaaedak-hitl
    restart: unless-stopped
    ports:
      - "9999:9999"
    volumes:
      - ./data:/usr/src/app/data
    env_file:
      - .env
